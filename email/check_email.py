import re

def check_email(email):

    error  = false
    if (re.search(".+@.+com"))
        raise ValueError("Email read from file isn't in the correct format")
    elif not(re.search(".+@.+com\s*.")):
        raise ValueError("The file has two or more email address in the same line")
    
